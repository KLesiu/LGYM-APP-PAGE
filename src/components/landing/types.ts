export type PreviewScreen = {
  kickerKey: string;
  titleKey: string;
  image: string;
  previewLayout?: "wide";
};

export type TrainerScreenSlot = {
  kickerKey: string;
  titleKey: string;
  placeholderKey: string;
  descriptionKey: string;
  darkImage: string;
  lightImage: string;
};

export type EloShowcase = {
  kickerKey: string;
  titleKey: string;
  descriptionKey: string;
  image: string;
};
